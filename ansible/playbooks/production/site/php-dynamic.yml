---
- name: Create Dynamic PHP Site
  hosts: production

  vars:
    dynamic_php: yes
  vars_prompt:
    - name:    domain
      prompt:  Domain Name (e.g. codeup.com)
      private: no

  roles:
    - PHP
    - Git-Hook
